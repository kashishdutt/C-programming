#include <stdio.h>

int main(){

    // Augmented assignment operator = used to replace a statement where an operator
    //                                 takes a variable as one of its arguments
    //                                 and then assigns the result back to the same variable
    //                                 x = x + 1
    //                                 x+=1
  
    int x = 12;

    //x = x + 2;
    //x+=2; 

    printf("%d", x);

    //similary we can use this in any arithmetic operator


    return 0;
}